# Runtime data not meant to be committed (see CLAUDE.md and
# specs/SPEC_github_workflow.md for what IS committed:
# data/jobs_seen.csv, data/digests/*.md, data/scraper_health.csv)
data/logs/
data/job_board.html
data/job_board.csv
data/NEW_JOBS_COUNT

.venv/
__pycache__/
*.pyc
.DS_Store

# Saved page dumps from manual endpoint research. These are verbatim copies of
# third-party job board pages, kept only as local scratch while working out a
# scraper. Not committed: the repo is public, and republishing scraped page
# content is a different matter from fetching it for personal use.
/*.html
/*.rss
/resp.json
cookies.txt

# Carried over from the GitHub Python template that was in the remote's
# initial commit, trimmed to what this project can actually produce.
.env
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
